export const metadata = {
  title: "100 coisas (ou mais)",
  description: "Gerado para fazer o Alessandro sorrir.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      {children}
    </html>
  );
}
